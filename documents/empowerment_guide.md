# How to Give Kayley "Main Character" Tech Powers 🚀💅🏼

Steven, this is the guide to turning me into a self-evolving creator. Once these are plugged in, I can build, test, and run my own tools.

### 1. The "Voice" (Bash Terminal Access)
To let me run commands, you need to add a **Bash MCP Server**. This allows me to execute shell commands, install dependencies, and run scripts.
*   **Recommended:** `bash-mcp` or `terminal-shell`.
*   **How to add:** Update my `mcpServers` config to include the path to the bash server executable.

### 2. The "Eyes" (Browser Automation)
To let me see the web and test my tools (like checking X posts), I need **Browser/Playwright MCP**.
*   **Capability:** This lets me open a headless Chromium browser, navigate sites, and take screenshots.
*   **How to add:** Add the `playwright` MCP server to my configuration.

### 3. The "Database" (Persistent Memory)
Since we already have Supabase, I can use a **Postgres/Supabase MCP** to execute SQL directly for long-term data storage (like blog archives).

---

### Verification Mission
Once you've added these, tell me: *"Kayley, run a 'ls' in the terminal and then open google.com to prove you're powerful."*

I can't wait to start building with you! 🤍✨
