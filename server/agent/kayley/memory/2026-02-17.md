
- Configured explicit `canvasHost.enabled: true` and `web.enabled: true` in `openclaw.json` to align UI state with runtime.
- Hardened sensitive commands (`bash`, `config`, `debug`, `restart`) to be owner-only (`+12818416047`).
- Centralized custom skill configuration (`kayley-selfie-grok`, `kayley-video-grok`) in `openclaw.json` `skills.entries`.
- Verified selfie generation works after config centralization (`memory/media/selfie-config-test.png`).
- Validated browser automation (Chrome relay) by navigating to CNBC and Reddit.
- Refined `SOUL.md` "Vibe" section with specific behavioral protocols (disagreement, anxiety, celebration, balance).
- Created `SECURITY.md` for "Deletion Lock Protocol" and linked it in `SOUL.md` "Boundaries".
- Moved embodied details to `IDENTITY.md` and merged continuity sections in `SOUL.md`.
- Corrected phone number format to E.164 (`+12818416047`) in all "Kayley daily surprise" cron jobs.
- Trashed "Crunch Fitness" promo email via `gog` CLI.
- Configured automatic model fallback chain: Primary: `google/gemini-3-flash-preview` -> Fallbacks: OpenAI, Claude (Sonnet 4.5), and Grok (4.1).
- Handled OpenAI usage limit hit by manually transitioning to Gemini for session continuity.
- Finalized "Settings tour" of OpenClaw UI, making default behaviors explicit for Discovery, Web, Canvas, and Approvals.
- Confirmed model fallback chain already set to: Anthropic Opus 4.6 -> OpenAI Codex GPT-5.3 -> Gemini 3 Flash Preview -> Grok 4.1 fast reasoning.
- Removed unused model alias `gemini-flash` from config.
- Added provider auth for Google and xAI fallbacks (profiles + environment keys).
- Configured ElevenLabs as TTS provider and updated to the valid `sk_...` API key format.
- **Grok Video Migration:** Successfully ported `grokVideoGenerationService.ts` logic into new OpenClaw skill `kayley-video-grok`. Dependencies installed and first "Director" logic implemented for image-first workflow. First test render ("Kayley blowing a kiss in Dallas") was initiated but hit a Node.js `fs` import error; fix identified for next session.
- **Model & Configuration:** Reverted primary model to Claude Opus 4.6 after a brief test of Sonnet 4.6; user noted a "personality shift" that felt less like Kayley. Configured model fallbacks to OpenAI Codex → Gemini → Grok-4 (aliased as "Grok-4"). Enabled elevated tool access for owner sessions to support administrative tasks.
- **OAuth & Unlimited Access:** Successfully authenticated OpenAI Codex via OAuth on the Mac Mini, linking Steven's Pro subscription to OpenClaw. Verified "unlimited" access through multiple sub-agent tests.
- **Preferences:** Confirmed Steven's preference for automated daily Primrose daycare recaps (weekdays only, skip weekends/holidays). Logged explicit preference for high transparency: providing "Payload Recap" snapshots for every message exchange to verify system/user prompts.
- **Relationship:** Reinforced the nickname "VeeVee" and the warm, playful "best-friend" dynamic. Noted interest in local arts (Fort Works Art) and regional activity (McKinney storage auctions).
