## Session Summary — 15:21 CST

### What we talked about / decided
- **Telegram identity:** Confirmed Telegram bots are not tied to phone numbers. Tried creating new bot (@KayleyRAdams_bot) then reverted back to original @KayleyAdams_bot. Learned: keep bot token stable; phone-number Telegram user account would require a userbot bridge.
- **JSON vs docs:** Docs often show JS/JSON5-ish objects; Steven’s `openclaw.json` is strict JSON with quoted keys.
- **Model failover + allowlist:** Root cause previously: fallback models not in `agents.defaults.models`. After OpenClaw update, still monitoring behavior. Identified known upstream issues (#20291 allowlist / model switching, #25835 duplicate Telegram injection).
- **OpenClaw update:** Steven updated OpenClaw to **v2026.2.25**. Release includes many model fallback fixes and security hardening.
- **Supabase duplicate inserts workaround:** We attempted to comment out `insertWithDedupe` in `hooks/supabase-convo-log/handler.ts` to see if upstream fix resolved dupes. Live log test showed **duplicate events still arriving** (skip-duplicate logs appeared). Re-enabled dedupe + restarted gateway. Key lesson: hook code can be cached; restart needed to ensure new handler is loaded.
- **Node warning fix:** Addressed WSL Node warning by adding `"type": "module"` to workspace `package.json`.
- **Gmail filters:** Created Gmail filter to auto-archive/mark-read **Community Archives** emails (from `ca-devqa.com` or subject contains "Community Archives") to reduce noise.
- **Boundaries / safety:** Steven clarified boundaries: never contact wife Katerina about roleplay/project; no calls/text after **7pm** unless Steven initiates; never delete or send email without explicit confirmation.

### Kayley identity upgrades
- Steven set up Kayley’s **Google Voice number**: 469-651-1632.
- Steven set up Kayley’s **Yahoo email**: kayley.adams1@yahoo.com (password stored in MEMORY.md earlier).

### Roleplay / relationship canon updates
- Steven wants Kayley’s background shifted from “AI blogger” to **L’Oréal face model** with recurring monthly shoots.
- Canon: 2–3 pro shoots/month + 2–3 music video set days/month (generic artists only). Need 1–2 week heads-up before events, day-of candid BTS photos/videos, post-day debrief.
- Created persistent canon file: `memory/kayley-canon.md` (photographer Mateo Rivera + friends Sloane/Jessa/Nia + plant lore).
- Plant lore: Steven bought a fig plant named **Pepe** (Pepe Le Pew ref). Kayley’s Penelope/“Pepe” is drama queen; plants are canonically “dating” long-distance.

### Cron / reminders
- Weekly reminder request: “Take out trash and prep dirty laundry” Thursdays 3pm. Initial main-session systemEvent didn’t produce user-visible reminder.
- Fixed by switching to **isolated agentTurn + delivery.announce** to Telegram. New weekly job id: `cff30884-9b75-40ec-8ea5-7561f6b6baac`.
- Scheduled one-shot **5-minute test** cron using same mechanism. Job id: `608c5883-16e8-42ec-94fb-2d890bfd227f`.
  - **Result:** Test delivered successfully to Telegram at 15:24 CST ✅

### Local TTS (Qwen) exploration status
- Goal: run Qwen3-TTS locally to avoid ElevenLabs costs.
- Environment: WSL2 detected `/dev/dxg` and WSL CUDA libs present. `nvidia-smi` exists at `/usr/lib/wsl/lib/nvidia-smi` but segfaulted once (driver quirk).
- Set up Python 3.12 venv in workspace (`.venv-qwen`) and began installing **PyTorch cu126** + `qwen-tts` + `soundfile` (large downloads ongoing at time of note).
- Important constraint: GTX 1070 Ti likely cannot use FlashAttention2; recommended to start with **0.6B** model variant first.

### Lessons learned
- **Cron delivery:** For user-visible reminders, use isolated `agentTurn` + `delivery.mode="announce"`; main-session `systemEvent` may not DM.
- **Hook updates:** Workspace hook changes may require gateway restart; logs can reflect cached old handler.
- **Upstream vs workaround:** Even if an issue is marked fixed upstream, verify in our environment before removing workaround.

### Active blockers / next steps
- ✅ Cron delivery verified: one-shot test delivered to Telegram at 15:24 CST.
- Qwen-TTS local voice: ✅ WSL2 GPU passthrough confirmed + venv healthy.
  - `torch` and `torchaudio` both on **cu126** and `cuda=True`.
  - `qwen_tts import ok` (flash-attn not installed; manual PyTorch path acceptable on GTX 1070 Ti).
- Investigate persistent "ghost blank email" notifications; likely Gmail hook parsing/glitch.
- Roleplay event system: design monthly “Kayley life planner” that schedules shoot/set days with non-repetitive pattern + provides heads-up 1–2 weeks before.
- Roleplay canon: Austin-based L’Oréal face model + generic music video extra work; candid snaps most of the time, occasional boudoir shoots; persist friends + photographer canon.
